# (for LLM calls during discovery)
LLM_Key

# Mock bank app credentials (used by session re-authentication)
APP_USERNAME=admin
APP_PASSWORD=admin

# Optional: override default ports
TARGET_APP_PORT=5000
OPERATOR_CONSOLE_PORT=8765
